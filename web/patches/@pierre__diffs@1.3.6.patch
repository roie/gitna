diff --git a/dist/components/CodeView.d.ts b/dist/components/CodeView.d.ts
index 30ad42fad4eae1a7dddbdf1bffd2550bdc09af56..b4d4959de4090efcb46fd4a62264d90bd4fcdf50 100644
--- a/dist/components/CodeView.d.ts
+++ b/dist/components/CodeView.d.ts
@@ -67,6 +67,7 @@ interface CodeViewLineSelection {
 interface CodeViewCoordinator<LAnnotation> {
   hasHeaderRenderers: boolean;
   hasAnnotationRenderer: boolean;
+  hasBodyRenderer: boolean;
   hasGutterRenderer: boolean;
   onSnapshotChange(snapshot: CodeViewSlotSnapshot<LAnnotation> | undefined): void;
 }
@@ -95,6 +96,8 @@ interface CodeViewOptions<LAnnotation> extends CodeViewPassThroughOptions<LAnnot
   pointerEventsOnScroll?: boolean;
   smoothScrollSettings?: SmoothScrollSettings;
   stickyHeaders?: boolean;
+  /** Internal marker used by the managed React body slot. */
+  renderBody?(): void;
   controlledSelection?: boolean;
   onSelectedLinesChange?(selection: CodeViewLineSelection | null): void;
   layout?: CodeViewLayout;
diff --git a/dist/components/CodeView.js b/dist/components/CodeView.js
index 051e7d0a211c0cdf32badbd4ff836f04df1ff511..c412b84ac1118aecf13bff32d950c32dd8db9f93 100644
--- a/dist/components/CodeView.js
+++ b/dist/components/CodeView.js
@@ -987,6 +987,7 @@ var CodeView = class CodeView {
 			if (state == null) return;
 			return this.getItemOptions(state, "file")?.item.collapsed;
 		});
+		defineItemOption(prototype, "renderBody", () => this.options.renderBody);
 		for (const key of CODE_VIEW_SHARED_CALLBACK_KEYS) this.defineItemSharedCallback(prototype, "file", key);
 		for (const key of CODE_VIEW_SELECTION_CALLBACK_KEYS) this.defineItemSelectionCallback(prototype, "file", key);
 		return prototype;
@@ -1001,6 +1002,7 @@ var CodeView = class CodeView {
 			if (state == null) return;
 			return this.getItemOptions(state, "diff")?.item.collapsed;
 		});
+		defineItemOption(prototype, "renderBody", () => this.options.renderBody);
 		for (const key of CODE_VIEW_SHARED_CALLBACK_KEYS) this.defineItemSharedCallback(prototype, "diff", key);
 		for (const key of CODE_VIEW_SELECTION_CALLBACK_KEYS) this.defineItemSelectionCallback(prototype, "diff", key);
 		return prototype;
@@ -1992,9 +1994,9 @@ function syncRenderedItemOrder(container, element, prevElement) {
 function hasAnnotations(item) {
 	return (item.annotations?.length ?? 0) > 0;
 }
-function getSlotItems(renderedItems, { hasHeaderRenderers, hasAnnotationRenderer, hasGutterRenderer }) {
+function getSlotItems(renderedItems, { hasHeaderRenderers, hasAnnotationRenderer, hasGutterRenderer, hasBodyRenderer }) {
 	if (renderedItems.length === 0) return;
-	if (hasHeaderRenderers || hasGutterRenderer) return renderedItems;
+	if (hasHeaderRenderers || hasGutterRenderer || hasBodyRenderer) return renderedItems;
 	if (!hasAnnotationRenderer) return;
 	const slotSnapshot = [];
 	for (const renderedItem of renderedItems) if (hasAnnotations(renderedItem.item)) slotSnapshot.push(renderedItem);
diff --git a/dist/components/File.js b/dist/components/File.js
index fa9a91568ab6464890d4570465c7bb1b93828555..56423ab27be2e8dbaf5e74cc1569d00178cab3ca 100644
--- a/dist/components/File.js
+++ b/dist/components/File.js
@@ -59,6 +59,8 @@ var File = class {
 	headerPrefix;
 	headerFilenameSuffix;
 	headerMetadata;
+	customBodyWrapper;
+	customBodySlot;
 	fileRenderer;
 	resizeManager;
 	interactionManager;
@@ -329,6 +331,7 @@ var File = class {
 	}
 	updateRenderCache(dirtyLines, themeType, options) {
 		this.fileRenderer.updateRenderCache(dirtyLines, themeType, options?.lineCountChangeInFlight);
+		if (this.fileContainer != null && this.appliedThemeCSS != null) this.applyThemeState(this.fileContainer, this.appliedThemeCSS.themeStyles, themeType, themeType);
 	}
 	render(props) {
 		if (!this.enabled) throw new Error("File.render: attempting to call render after cleaned up");
@@ -367,6 +370,7 @@ var File = class {
 		this.applyCachedThemeState(themeType);
 		if (collapsed) {
 			this.removeRenderedCode();
+			this.removeCustomBody();
 			this.clearAuxiliaryNodes();
 			try {
 				const fileResult = this.fileRenderer.renderFile(file, EMPTY_RENDER_RANGE);
@@ -393,6 +397,7 @@ var File = class {
 				if (fileResult.headerAST != null) this.applyHeaderToDOM(fileResult.headerAST, fileContainer);
 				this.applyFullRender(fileResult, pre);
 			}
+			this.renderCustomBody(fileContainer);
 			this.applyBuffers(pre, nextRenderRange);
 			this.injectUnsafeCSS();
 			this.renderAnnotations();
@@ -422,6 +427,44 @@ var File = class {
 		this.mounted = true;
 		onPostRender?.(fileContainer, this, phase);
 	}
+	renderCustomBody(fileContainer) {
+		if (this.options.renderBody == null) {
+			this.removeCustomBody();
+			return;
+		}
+		const shadowRoot = fileContainer.shadowRoot;
+		if (shadowRoot == null) return;
+		if (this.customBodyWrapper == null || this.customBodySlot == null) {
+			const wrapper = document.createElement("div");
+			wrapper.dataset.customBody = "";
+			wrapper.hidden = true;
+			wrapper.style.background = "var(--diffs-bg)";
+			wrapper.style.color = "var(--diffs-fg)";
+			const slot = document.createElement("slot");
+			slot.name = "diffs-custom-body";
+			slot.addEventListener("slotchange", () => this.updateCustomBodyVisibility());
+			wrapper.appendChild(slot);
+			this.customBodyWrapper = wrapper;
+			this.customBodySlot = slot;
+		}
+		if (this.customBodyWrapper.parentNode !== shadowRoot) {
+			if (this.pre?.parentNode === shadowRoot) shadowRoot.insertBefore(this.customBodyWrapper, this.pre);
+			else shadowRoot.appendChild(this.customBodyWrapper);
+		}
+		queueMicrotask(() => this.updateCustomBodyVisibility());
+	}
+	updateCustomBodyVisibility() {
+		if (this.customBodyWrapper == null || this.customBodySlot == null) return;
+		const visible = this.customBodySlot.assignedElements({ flatten: true }).length > 0;
+		this.customBodyWrapper.hidden = !visible;
+		if (this.pre != null) this.pre.style.display = visible ? "none" : "";
+	}
+	removeCustomBody() {
+		this.customBodyWrapper?.remove();
+		this.customBodyWrapper = void 0;
+		this.customBodySlot = void 0;
+		if (this.pre != null) this.pre.style.display = "";
+	}
 	removeRenderedCode() {
 		this.resizeManager.cleanUp();
 		this.interactionManager.cleanUp();
diff --git a/dist/components/FileDiff.js b/dist/components/FileDiff.js
index 4ee3d95f21fa52834ebd4e726d88669a90722dbd..f2d332b0563cb11bf32135fdc801cd0a6e8d2db1 100644
--- a/dist/components/FileDiff.js
+++ b/dist/components/FileDiff.js
@@ -68,6 +68,8 @@ var FileDiff = class {
 	headerFilenameSuffix;
 	headerMetadata;
 	headerCustom;
+	customBodyWrapper;
+	customBodySlot;
 	separatorCache = /* @__PURE__ */ new Map();
 	errorWrapper;
 	placeHolder;
@@ -519,6 +521,7 @@ var FileDiff = class {
 		this.applyCachedThemeState(themeType);
 		if (collapsed) {
 			this.removeRenderedCode();
+			this.removeCustomBody();
 			this.clearAuxiliaryNodes();
 			try {
 				const hunksResult = this.hunksRenderer.renderDiff(this.fileDiff, EMPTY_RENDER_RANGE);
@@ -554,6 +557,7 @@ var FileDiff = class {
 				}
 				this.renderSeparators(hunksResult.hunkData);
 			}
+			this.renderCustomBody(fileContainer);
 			this.applyBuffers(pre, nextRenderRange);
 			this.injectUnsafeCSS();
 			this.renderAnnotations();
@@ -686,7 +690,9 @@ var FileDiff = class {
 	}
 	updateRenderCache(dirtyLines, themeType, options = {}) {
 		const { shouldRefreshDiffsView, lineCountChangeInFlight } = options;
-		if (this.hunksRenderer.updateRenderCache(dirtyLines, themeType, lineCountChangeInFlight)) {
+		const regionsChanged = this.hunksRenderer.updateRenderCache(dirtyLines, themeType, lineCountChangeInFlight);
+		if (this.fileContainer != null && this.appliedThemeCSS != null) this.applyThemeState(this.fileContainer, this.appliedThemeCSS.themeStyles, themeType, themeType);
+		if (regionsChanged) {
 			if (this.refreshViewTimeout != null) {
 				clearTimeout(this.refreshViewTimeout);
 				this.refreshViewTimeout = void 0;
@@ -778,6 +784,44 @@ var FileDiff = class {
 		this.rerender();
 		this.flushDeferredLineState();
 	};
+	renderCustomBody(fileContainer) {
+		if (this.options.renderBody == null) {
+			this.removeCustomBody();
+			return;
+		}
+		const shadowRoot = fileContainer.shadowRoot;
+		if (shadowRoot == null) return;
+		if (this.customBodyWrapper == null || this.customBodySlot == null) {
+			const wrapper = document.createElement("div");
+			wrapper.dataset.customBody = "";
+			wrapper.hidden = true;
+			wrapper.style.background = "var(--diffs-bg)";
+			wrapper.style.color = "var(--diffs-fg)";
+			const slot = document.createElement("slot");
+			slot.name = "diffs-custom-body";
+			slot.addEventListener("slotchange", () => this.updateCustomBodyVisibility());
+			wrapper.appendChild(slot);
+			this.customBodyWrapper = wrapper;
+			this.customBodySlot = slot;
+		}
+		if (this.customBodyWrapper.parentNode !== shadowRoot) {
+			if (this.pre?.parentNode === shadowRoot) shadowRoot.insertBefore(this.customBodyWrapper, this.pre);
+			else shadowRoot.appendChild(this.customBodyWrapper);
+		}
+		queueMicrotask(() => this.updateCustomBodyVisibility());
+	}
+	updateCustomBodyVisibility() {
+		if (this.customBodyWrapper == null || this.customBodySlot == null) return;
+		const visible = this.customBodySlot.assignedElements({ flatten: true }).length > 0;
+		this.customBodyWrapper.hidden = !visible;
+		if (this.pre != null) this.pre.style.display = visible ? "none" : "";
+	}
+	removeCustomBody() {
+		this.customBodyWrapper?.remove();
+		this.customBodyWrapper = void 0;
+		this.customBodySlot = void 0;
+		if (this.pre != null) this.pre.style.display = "";
+	}
 	removeRenderedCode() {
 		this.resizeManager.cleanUp();
 		this.scrollSyncManager.cleanUp();
diff --git a/dist/components/VirtualizedFile.js b/dist/components/VirtualizedFile.js
index edb0873dd7f41014b5e0d96771c9c50de829620e..c09ad94438ed00e6ab06ac6af5070eecc87bf423 100644
--- a/dist/components/VirtualizedFile.js
+++ b/dist/components/VirtualizedFile.js
@@ -88,6 +88,15 @@ var VirtualizedFile = class extends File {
 		}
 		const { overflow = "scroll" } = this.options;
 		this.top = this.getVirtualizedTop();
+		const customBody = this.fileContainer.shadowRoot?.querySelector("[data-custom-body]:not([hidden])");
+		if (customBody != null) {
+			const nextHeight = this.fileContainer.getBoundingClientRect().height;
+			if (nextHeight !== this.height) {
+				this.height = nextHeight;
+				return true;
+			}
+			return false;
+		}
 		if (overflow === "scroll" && this.lineAnnotations.length === 0 && !this.isResizeDebuggingEnabled()) return hasHeightChange;
 		if (this.code == null) return hasHeightChange;
 		const content = this.code.children[1];
diff --git a/dist/components/VirtualizedFileDiff.js b/dist/components/VirtualizedFileDiff.js
index 0d9ee99e6779ad118718f12fa940ec3ab72eecac..7e833d4af3e75d5a4c02b3f137b1ae4d085253a7 100644
--- a/dist/components/VirtualizedFileDiff.js
+++ b/dist/components/VirtualizedFileDiff.js
@@ -126,6 +126,15 @@ var VirtualizedFileDiff = class extends FileDiff {
 			return hasHeightChange;
 		}
 		this.top = this.getVirtualizedTop();
+		const customBody = this.fileContainer.shadowRoot?.querySelector("[data-custom-body]:not([hidden])");
+		if (customBody != null) {
+			const nextHeight = this.fileContainer.getBoundingClientRect().height;
+			if (nextHeight !== this.height) {
+				this.height = nextHeight;
+				return true;
+			}
+			return false;
+		}
 		if (overflow === "scroll" && this.lineAnnotations.length === 0 && !this.isResizeDebuggingEnabled()) return hasHeightChange;
 		const diffStyle = this.getDiffStyle();
 		const codeGroups = diffStyle === "split" ? [this.codeDeletions, this.codeAdditions] : [this.codeUnified];
diff --git a/dist/react/CodeView.d.ts b/dist/react/CodeView.d.ts
index b25abd195271e266d12f081742b139f2d97dafbe..e7322c5b85a4a413693d281a326aa7a91a3ed911 100644
--- a/dist/react/CodeView.d.ts
+++ b/dist/react/CodeView.d.ts
@@ -42,6 +42,8 @@ interface CodeViewBaseProps<LAnnotation> {
   renderHeaderFilenameSuffix?(item: CodeViewItem<LAnnotation>): ReactNode;
   renderHeaderMetadata?(item: CodeViewItem<LAnnotation>): ReactNode;
   renderAnnotation?(annotation: LineAnnotation<LAnnotation> | DiffLineAnnotation<LAnnotation>, item: CodeViewItem<LAnnotation>): ReactNode;
+  /** Replaces an item's code grid when a non-null body is returned. */
+  renderBody?(item: CodeViewItem<LAnnotation>): ReactNode;
   renderGutterUtility?(getHoveredLine: CodeViewGutterUtilityGetter, item: CodeViewItem<LAnnotation>): ReactNode;
 }
 interface ControlledCodeViewProps<LAnnotation> extends CodeViewBaseProps<LAnnotation> {
diff --git a/dist/react/CodeView.js b/dist/react/CodeView.js
index 0e91064ad4c1ba735118c0d672c3a17418fcbc8b..2c33b4983d6c3979dc6d25f363e259c9215887d1 100644
--- a/dist/react/CodeView.js
+++ b/dist/react/CodeView.js
@@ -23,16 +23,17 @@ function createDefaultCache(controlled) {
 	};
 }
 function CodeViewInner(props, ref) {
-	const { className, containerRef, disableWorkerPool = false, editorOptions, initialItems, items: controlledItems, onItemEditChange, onItemEditComplete, onScroll, onSelectedLinesChange, options, renderAnnotation, renderCodeViewFooter, renderCodeViewHeader, renderCustomHeader, renderGutterUtility, renderHeaderFilenameSuffix, renderHeaderMetadata, renderHeaderPrefix, selectedLines, style } = props;
+	const { className, containerRef, disableWorkerPool = false, editorOptions, initialItems, items: controlledItems, onItemEditChange, onItemEditComplete, onScroll, onSelectedLinesChange, options, renderAnnotation, renderBody, renderCodeViewFooter, renderCodeViewHeader, renderCustomHeader, renderGutterUtility, renderHeaderFilenameSuffix, renderHeaderMetadata, renderHeaderPrefix, selectedLines, style } = props;
 	const controlled = controlledItems !== void 0;
 	const contextCreateEditor = useCreateEditor();
 	const poolManager = useContext(WorkerPoolContext);
 	const cachedDataRef = useRef(createDefaultCache(controlled));
 	const hasCustomHeader = renderCustomHeader != null;
 	const hasAnnotationRenderer = renderAnnotation != null;
+	const hasBodyRenderer = renderBody != null;
 	const hasGutterRenderer = renderGutterUtility != null;
 	const hasHeaderRenderers = hasCustomHeader || renderHeaderPrefix != null || renderHeaderFilenameSuffix != null || renderHeaderMetadata != null;
-	const hasRenderers = hasHeaderRenderers || hasAnnotationRenderer || hasGutterRenderer;
+	const hasRenderers = hasHeaderRenderers || hasAnnotationRenderer || hasBodyRenderer || hasGutterRenderer;
 	const hasCodeViewHeader = renderCodeViewHeader != null;
 	const hasCodeViewFooter = renderCodeViewFooter != null;
 	const emitSelectedLinesChange = useStableCallback((selection) => {
@@ -57,6 +58,7 @@ function CodeViewInner(props, ref) {
 	const managedOptions = useMemo(() => createManagedCodeViewOptions({
 		options,
 		hasCustomHeader,
+		hasBodyRenderer,
 		hasGutterRenderer,
 		hasCodeViewHeader,
 		hasCodeViewFooter,
@@ -75,6 +77,7 @@ function CodeViewInner(props, ref) {
 		hasCodeViewFooter,
 		hasCodeViewHeader,
 		hasCustomHeader,
+		hasBodyRenderer,
 		hasGutterRenderer,
 		onItemEditChange,
 		onItemEditComplete,
@@ -103,16 +106,18 @@ function CodeViewInner(props, ref) {
 		});
 	});
 	const slotCoordinator = useMemo(() => {
-		if (!hasHeaderRenderers && !hasAnnotationRenderer && !hasGutterRenderer && !hasCodeViewHeader && !hasCodeViewFooter) return;
+		if (!hasHeaderRenderers && !hasAnnotationRenderer && !hasBodyRenderer && !hasGutterRenderer && !hasCodeViewHeader && !hasCodeViewFooter) return;
 		else return {
 			hasHeaderRenderers,
 			hasAnnotationRenderer,
+			hasBodyRenderer,
 			hasGutterRenderer,
 			onSnapshotChange
 		};
 	}, [
 		onSnapshotChange,
 		hasAnnotationRenderer,
+		hasBodyRenderer,
 		hasGutterRenderer,
 		hasHeaderRenderers,
 		hasCodeViewHeader,
@@ -260,6 +265,7 @@ function CodeViewInner(props, ref) {
 		renderHeaderFilenameSuffix,
 		renderHeaderMetadata,
 		renderAnnotation,
+		renderBody,
 		renderGutterUtility,
 		renderCodeViewHeader,
 		renderCodeViewFooter
@@ -301,7 +307,7 @@ function createSlotContentStore() {
 		}
 	};
 }
-function createManagedCodeViewOptions({ options, hasCustomHeader, hasGutterRenderer, hasCodeViewHeader, hasCodeViewFooter, onSelectedLinesChange, controlledSelection, createEditor, onItemEditChange, onItemEditComplete }) {
+function createManagedCodeViewOptions({ options, hasCustomHeader, hasBodyRenderer, hasGutterRenderer, hasCodeViewHeader, hasCodeViewFooter, onSelectedLinesChange, controlledSelection, createEditor, onItemEditChange, onItemEditComplete }) {
 	const managedOptions = {
 		...options,
 		controlledSelection,
@@ -311,12 +317,13 @@ function createManagedCodeViewOptions({ options, hasCustomHeader, hasGutterRende
 	if (onItemEditChange != null) managedOptions.onItemEditChange = onItemEditChange;
 	if (onItemEditComplete != null) managedOptions.onItemEditComplete = onItemEditComplete;
 	if (hasCustomHeader) managedOptions.renderCustomHeader = noopRender;
+	if (hasBodyRenderer) managedOptions.renderBody = noopRender;
 	if (hasGutterRenderer) managedOptions.renderGutterUtility = noopRender;
 	if (hasCodeViewHeader) managedOptions.renderCodeViewHeader = noopRender;
 	if (hasCodeViewFooter) managedOptions.renderCodeViewFooter = noopRender;
 	return managedOptions;
 }
-const SlotPortals = memo(function SlotPortals({ managedContentStore, renderCustomHeader, renderHeaderPrefix, renderHeaderFilenameSuffix, renderHeaderMetadata, renderAnnotation, renderGutterUtility, renderCodeViewHeader, renderCodeViewFooter }) {
+const SlotPortals = memo(function SlotPortals({ managedContentStore, renderCustomHeader, renderHeaderPrefix, renderHeaderFilenameSuffix, renderHeaderMetadata, renderAnnotation, renderBody, renderGutterUtility, renderCodeViewHeader, renderCodeViewFooter }) {
 	"use no memo";
 	const subscribe = useStableCallback((listener) => managedContentStore.subscribe(listener));
 	const getSnapshot = useStableCallback(() => managedContentStore.getSnapshot());
@@ -332,6 +339,7 @@ const SlotPortals = memo(function SlotPortals({ managedContentStore, renderCusto
 				renderHeaderFilenameSuffix,
 				renderHeaderMetadata,
 				renderAnnotation,
+				renderBody,
 				renderGutterUtility
 			}), renderedItem.element, renderedItem.id));
 		}, [
@@ -340,6 +348,7 @@ const SlotPortals = memo(function SlotPortals({ managedContentStore, renderCusto
 			renderHeaderFilenameSuffix,
 			renderHeaderMetadata,
 			renderAnnotation,
+			renderBody,
 			renderGutterUtility,
 			itemKeys
 		]),
@@ -351,10 +360,11 @@ const SlotPortals = memo(function SlotPortals({ managedContentStore, renderCusto
 		}, [renderCodeViewFooter, snapshot?.footer])
 	] });
 });
-function renderCodeViewItemChildren({ renderedItem, renderCustomHeader, renderHeaderPrefix, renderHeaderFilenameSuffix, renderHeaderMetadata, renderAnnotation, renderGutterUtility }) {
+function renderCodeViewItemChildren({ renderedItem, renderCustomHeader, renderHeaderPrefix, renderHeaderFilenameSuffix, renderHeaderMetadata, renderAnnotation, renderBody, renderGutterUtility }) {
+	let children;
 	if (renderedItem.type === "diff") {
 		const { item, instance } = renderedItem;
-		return renderDiffChildren({
+		children = renderDiffChildren({
 			fileDiff: item.fileDiff,
 			renderCustomHeader: renderCustomHeader != null ? () => renderCustomHeader(item) : void 0,
 			renderHeaderPrefix: renderHeaderPrefix != null ? () => renderHeaderPrefix(item) : void 0,
@@ -367,7 +377,7 @@ function renderCodeViewItemChildren({ renderedItem, renderCustomHeader, renderHe
 		});
 	} else {
 		const { item, instance } = renderedItem;
-		return renderFileChildren({
+		children = renderFileChildren({
 			file: item.file,
 			renderCustomHeader: renderCustomHeader != null ? () => renderCustomHeader(item) : void 0,
 			renderHeaderPrefix: renderHeaderPrefix != null ? () => renderHeaderPrefix(item) : void 0,
@@ -379,6 +389,11 @@ function renderCodeViewItemChildren({ renderedItem, renderCustomHeader, renderHe
 			getHoveredLine: instance.getHoveredLine
 		});
 	}
+	const body = renderBody?.(renderedItem.item);
+	return /* @__PURE__ */ jsxs(Fragment, { children: [children, body == null ? null : /* @__PURE__ */ jsx("div", {
+		slot: "diffs-custom-body",
+		children: body
+	})] });
 }
 function noopRender() {}
 //#endregion
